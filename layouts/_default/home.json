[{{ range $index, $page := where .Site.Pages "Type" "not in"  (slice "page" "json") }}
 {{ if ne .Section "tags"}}
 {{ if ne .Section "projektfilter"}}
 {{ if ne $page.Type "xml"}}
   {{ if ne $index 0 }},{{ end }}
     {
       "url": "{{ $page.Permalink }}",
       "title": {{ $page.Title | jsonify}},
       "section": "{{ .Section }}",
       "date": "{{ $page.Date }}",
       {{ if and (isset $page.Params "img") (eq .Section "projekte") }}
       "img": "{{ $page.Params.img }}",
       {{ end }}
       {{ $downloads := ""}}
       {{ if eq $page.Layout "verein"}}
       {{ $downloads := (slice "Tätigkeitsbericht" "Satzung" "Governance Modell" "Mission Statement" "Memorandum of Understanding") }}
       {{ end }}
       "content": {{ (slice $downloads $page.PlainWords) | jsonify }}
     }
 {{ end }}
 {{ end }}
 {{ end }}
 {{ end }}]
